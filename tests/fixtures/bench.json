{
    "version":3,
    "host":["http://localhost:8080"],
    "read_header_timeout":"200ms",
    "extra_config": {
        "telemetry/logging": {
            "level": "CRITICAL",
            "prefix": "[KRAKEND]"
        }
    },
    "endpoints": [{
        "endpoint": "/test",
        "backend": [{
            "url_pattern": "/__health"
        }]
    }]
}